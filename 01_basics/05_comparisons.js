 console.log(null > 0); //false as it converts null int   o 0 and 0 is not greater than 0
 console.log(null == 0); //false as it doesn't convert null into 0 for comparison
 console.log(null >= 0); //true as it converts null into 0 and 0 is not greater than or equal to 0
 console.log(null <= 0); //true as it converts null into 0 and 0 is less than or equal to 0


 console.log("2" == 2);    //true as it checks only the value 
 console.log("2" === 2);   //false as it checks both the value and the data type
        